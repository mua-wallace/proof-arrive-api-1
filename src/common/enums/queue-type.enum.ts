/**
 * Queue type enum
 * Represents the type of queue a vehicle can be in
 */
export enum QueueType {
  LOADING = 'LOADING', // Queue for loading operations
  UNLOADING = 'UNLOADING', // Queue for unloading operations
}
