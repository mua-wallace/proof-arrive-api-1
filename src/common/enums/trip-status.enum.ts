/**
 * Trip status enum
 * Tracks the status of a vehicle trip between centers
 */
export enum TripStatus {
  ONGOING = 'ONGOING', // Trip is in progress
  COMPLETED = 'COMPLETED', // Trip has been completed
}
